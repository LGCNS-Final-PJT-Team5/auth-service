spring:
  profiles:
    active: dev   # dev profile? ??
  application:
    name: auth-service
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      profile: dev    # dev profile ?? ?? ??
      label: main